{
  "name": "rebook-extension-template",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "vite build",
    "typecheck": "tsc --noEmit",
    "pack:extension": "npm run build && node scripts/pack-extension.mjs"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "fflate": "^0.8.2",
    "rebook": "^0.8.0",
    "typescript": "^7.0.0",
    "vite": "^8.0.0"
  }
}
